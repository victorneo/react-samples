import React from 'react';
import ReactDOM from 'react-dom/client';
import Sample1 from './s1/sample1';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Sample1 />
  </React.StrictMode>
);
